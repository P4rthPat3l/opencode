pluginGroup=ai.opencode.p4rth
pluginName=P4rth OpenCode
pluginVersion=2.0.0
# Compile/baseline platform. sinceBuild is the oldest supported IDE (2024.2). There is no
# untilBuild cap (see build.gradle.kts) so the plugin stays compatible with future IDE majors.
pluginSinceBuild=242
platformVersion=2024.2.5
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
